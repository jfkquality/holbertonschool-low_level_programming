#include <stdio.h>
#include <string.h>
#include "header.h"

int main(void)
{
	char name=char[20];
	int length=strlen(name);
	i=0;

	for (i=0; i<length;i++)
	{
		putchar("%s",name[i]);
	}
	putchar("\n");
}
