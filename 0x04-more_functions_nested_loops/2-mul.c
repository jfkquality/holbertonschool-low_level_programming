#include <stdio.h>
#include "holberton.h"

/**
 * mul - multiplies 2 numbers
 * @a: The first number
 * @b: The 2nd number
 * @c: The product
 * Return: product.
 * On error, -1 is returned, and errno is set appropriately.
 */

int mul(int a, int b)
{
	return (a * b);
}
