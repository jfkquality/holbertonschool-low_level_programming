#include "holberton.h"
#include (stdio.h>

/**
 * print_numbers(void) - print number 0-9
 * Return: void
 */
void print_numbers(void)
{
	for (i = 0; i < 10; i++)
		if (i == 2 || i == 4)
			continue;
		else
			_putchar(i);
	_putchar("\n");
}
